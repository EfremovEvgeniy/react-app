import React from 'react';

const Header = () => {
    return (
      <div>
        <a href="#">Home</a>
        <a href="#">New feed</a>
        <a href="#">New messages</a>
      </div>
    );
  }

export default Header;